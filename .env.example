# Environment variables for ninadon
# Copy this file to .env and fill in your actual values

# Required: OpenRouter API key for AI summarization and image analysis
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Required for posting to Mastodon (not needed for --dry runs)
MASTODON_ACCESS_TOKEN=your_mastodon_access_token_here
MASTODON_BASE_URL=https://mastodon.social

# Optional: Custom prompts
# SYSTEM_PROMPT=Summarize the following video transcript, description, and account name. Additionally, create a detailed video description for visually impaired people (up to 1400 characters). Respond with valid JSON: {"summary": "your summary", "video_description": "description for visually impaired"}
# USER_PROMPT=
# CONTEXT_PROMPT=Analyze the following video history and create a concise context summary that captures the user's content themes, interests, and patterns
# IMAGE_ANALYSIS_PROMPT=Analyze these photos from a tiktok clip, make a connection between the photos

# Optional: AI model configuration
# OPENROUTER_MODEL=tngtech/deepseek-r1t2-chimera:free
# ENHANCE_MODEL=google/gemini-2.5-flash-lite
# CONTEXT_MODEL=tngtech/deepseek-r1t2-chimera:free

# Optional: Transcoding settings
# ENABLE_TRANSCODING=false
# TRANSCODE_TIMEOUT=600
# MASTODON_MEDIA_TIMEOUT=600

# Optional: Data storage path (useful for local development)
# DATA_PATH=./data